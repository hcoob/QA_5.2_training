name: EG job

on:
  push:
    branches: ["main"]
  pull_request: 
    branches:  ["main"]

  workflow_dispatch:
  
jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: echo Hello
      - name: Run a multi-line script
        run:
          echo Add
          echo test
      
    
  

  build:

    runs-on: 
